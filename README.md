# polymer-external-dependencies
Some tests about polymer and how to handle external dependencies

# get dependencies
`bower install`

# Demo
## get demo dependencies
`npm install -d`

## run the demo
`grunt connect`
Then open `localhost:8080/demo` in your favorite web browser
